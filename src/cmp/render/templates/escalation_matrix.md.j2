# Escalation Matrix — {{ client_name }}

> **DRAFT — For consultant review. Not ISO certification.**

This matrix supports the [Crisis Response Playbook](crisis_playbook.md). Notification timeframes start when the site or programme lead **confirms** an incident.

## Severity levels

{% for level in governance.crisis_levels %}
### Level {{ level.level }} — {{ level.name }}

{{ level.description }}

**Example triggers:** {{ level.activation_triggers | join('; ') }}.

{% endfor %}

## Notification sequence

When an incident is confirmed, notify roles in this order. The crisis lead may parallelise notifications at Level 3+.

| Level | Within | Roles notified | Channel | Crisis lead action |
|-------|--------|----------------|---------|-------------------|
{% for step in governance.escalation_matrix %}
| {{ step.severity }} | {{ step.notify_within_minutes }} min | {{ step.roles | join(', ') }} | {{ step.channel }} | Confirm level; convene activated roles per scenario guide |
{% endfor %}

## Decision authorities

{% for d in governance.decision_authorities %}
**{{ d.decision_type }}** — Primary: {{ d.authority_role }}{% if d.backup_role %}; Backup: {{ d.backup_role }}{% endif %}{% if d.notes %}. {{ d.notes }}{% endif %}

{% endfor %}

## Role roster (fill before go-live)

| Role | Alternate | Activates at |
|------|-----------|--------------|
{% for role in playbook.roles %}
| {{ role.role }} | {{ role.alternate_role or '—' }} | {{ role.activates_when }} |
{% endfor %}

{% if discovery and discovery.organization_context and discovery.organization_context.flexibility_notes %}
## Organizational notes

{% for note in discovery.organization_context.flexibility_notes %}
- {{ note }}
{% endfor %}
{% endif %}

**Consultant action:** Insert named individuals, 24/7 phone numbers, and backup contacts before client delivery.
